import React from 'react';
import './style.css'

// stateless
const Header = (props) => (
    <div className="header-container">
        <span>GitHub - API- ReactJS</span>
    </div>
)

export default Header;
